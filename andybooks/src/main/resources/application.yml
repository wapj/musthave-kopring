spring:
  application:
    name: andybooks

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always

#debug: true

example:
  message:
    prefix: ">>> "
    text: "Hello, Auto Configuration!"
